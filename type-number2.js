// salvar em uma var o resultado de uma operação

const soma = 10 + 10;
const multiplicacao = 10 * 10;
const operacao = (soma + multiplicacao) * 10;

console.log(operacao)

// o que está parenteses é executado primeiro
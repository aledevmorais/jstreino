// conversão de textos e booleanos atraves do number

let largura = "10";
let altura = "5";

console.log(Number(largura) * Number(altura));

// pode multiplicar, somar, dividir estrair depois da atribuição

// utilizar no js conversões de forma explicita
